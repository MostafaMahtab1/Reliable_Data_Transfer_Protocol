# Reliable_Data_Transfer_Protocol
A custom reliable file transfer protocol built on top of UDP. It provides reliable, ordered, and corruption-free delivery of data over an unreliable network by implementing TCP-inspired mechanisms at the application layer.
